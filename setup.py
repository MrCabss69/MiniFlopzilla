import setuptools

setuptools.setup(
    name="miniflopzilla",
    version="0.1",
    description="GUI to calc range vs range poker odds ",
    packages=setuptools.find_packages(include=['miniflopzilla*']),
    python_requires='>=3.7',
)
